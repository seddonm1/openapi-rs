[package]
name = "entity_macro"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
convert_case = "0.6.0"
quote = "1.0.36"
syn = { version = "2.0.73", features = ["full", "parsing"] }

[lints]
workspace = true
